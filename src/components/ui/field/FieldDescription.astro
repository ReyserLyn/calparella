---
import { cn } from '@/lib/utils'

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<p class={cn('text-sm text-muted-foreground/75', className)}>
  <slot />
</p>
