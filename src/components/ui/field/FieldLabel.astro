---
import { cn } from '@/lib/utils'

interface Props {
  for?: string
  class?: string
}

const { for: forAttr, class: className } = Astro.props
---

<label for={forAttr} class={cn('text-sm font-medium leading-none', className)}>
  <slot />
</label>
